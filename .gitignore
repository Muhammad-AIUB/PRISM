*.log
.DS_Store
Thumbs.db

# Local environment. .env still holds the credentials the deployed services
# need (database, Redis, GitHub OAuth, AI and mail keys) — do not commit it.
.env
.env.backup
.env.production

/.codex
/.cursor/
/.idea
/.nova
/.vscode
/.zed

# Each package keeps its own dependencies and build output.
node_modules
/prism-api/dist
/prism-web/.next
/prism-web/out
/mcp-server/package-lock.json
*.tsbuildinfo
next-env.d.ts
